function f_list(){
 echo "=== $FUNCNAME ST ==="
 
 while read line; do
  echo "line:$line:"
  v_rgx="^ *$"
  fi [[ $line =~ ^#.* ]] | [[ $line =~ $v_rgx ]] ;
  then
  
  else
  
  fi
 done < $(ls $2)
 
 echo "=== $FUNCNAME ST ==="
}
